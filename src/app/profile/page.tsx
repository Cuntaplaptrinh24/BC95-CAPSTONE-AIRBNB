// Trang hồ sơ cá nhân. Toàn bộ nội dung nằm trong ProfileContent,
// vì trang này cần biết ai đang đăng nhập nên phải chạy ở trình duyệt.

import ProfileContent from "@/components/profile/profile-content";

export default function ProfilePage() {
  return <ProfileContent />;
}
